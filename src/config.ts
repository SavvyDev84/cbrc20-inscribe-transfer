const config = {
  defaultOutput: 546,
  txRate: 50,
  mempoolNetwork: "testnet/",
  encodedAddressPrefix: "testnet",
  amount: 1,
  tick: "PLAY",
  max: 21000000,
  lim: 1000,
  dec: 8,
  mintAmount: 1000,
  transferAmount: 100,
};

export default config;
